<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <style>
        body {
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 50px;
            padding: 80px 20px;
            background-color: #ffffff;
        }

        .header-container {
            width: 800px; /* Format large pour Header Notion */
            filter: drop-shadow(0 15px 35px rgba(0,0,0,0.04));
        }

        svg {
            width: 100%;
            height: auto;
            display: block;
            shape-rendering: geometricPrecision;
        }

        /* Palettes de couleurs Aesthetic */
        .c-lavender { --bg: #e7def6; --pattern: rgba(75, 68, 83, 0.07); }
        .c-peach { --bg: #fbe4d8; --pattern: rgba(107, 84, 74, 0.07); }
        .c-sage { --bg: #e2f3e8; --pattern: rgba(74, 107, 88, 0.07); }
        .c-blue { --bg: #d8e8fb; --pattern: rgba(74, 91, 107, 0.07); }
        .c-cream { --bg: #fff9f0; --pattern: rgba(141, 110, 99, 0.07); }
    </style>
</head>
<body>

    <script>
        const themes = [
            {class: 'c-lavender', label: 'Lavender Mist'},
            {class: 'c-peach', label: 'Soft Peach'},
            {class: 'c-sage', label: 'Quiet Sage'},
            {class: 'c-blue', label: 'Cloud Blue'},
            {class: 'c-cream', label: 'Vanilla Cream'}
        ];

        themes.forEach(theme => {
            document.body.innerHTML += `
                <div class="header-container ${theme.class}">
                    <svg viewBox="0 0 800 300" xmlns="http://www.w3.org/2000/svg">
                        <defs>
                            <pattern id="grid-${theme.class}" x="0" y="0" width="30" height="30" patternUnits="userSpaceOnUse">
                                <path d="M 30 0 L 0 0 0 30" fill="none" stroke="var(--pattern)" stroke-width="1.5"/>
                            </pattern>
                        </defs>
                        <path d="M80 20 C68 20 60 28 60 40 V60 H40 C18 60 0 78 0 100 V260 C0 282 18 300 40 300 H760 C782 300 800 282 800 260 V100 C800 78 782 60 760 60 H180 V40 C180 28 172 20 160 20 H80 Z" fill="var(--bg)"/>
                        <path d="M80 20 C68 20 60 28 60 40 V60 H40 C18 60 0 78 0 100 V260 C0 282 18 300 40 300 H760 C782 300 800 282 800 260 V100 C800 78 782 60 760 60 H180 V40 C180 28 172 20 160 20 H80 Z" fill="url(#grid-${theme.class})"/>
                        <text x="400" y="170" text-anchor="middle" font-family="Arial, sans-serif" font-size="42" font-weight="bold" letter-spacing="25" fill="#4b4453" style="opacity: 0.8;">DEAP</text>
                        <text x="400" y="240" text-anchor="middle" font-family="Times New Roman, serif" font-size="20" letter-spacing="12" fill="#4b4453" style="font-variant-caps: all-small-caps; opacity: 0.5;">SŌMA</text>
                    </svg>
                </div>
            `;
        });
    </script>

</body>
</html>
