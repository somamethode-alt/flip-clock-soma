<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=EB+Garamond:wght@400;500&display=swap');

        :root {
            --bg-page: #FDFBF7;
            --ink: #3D2B1F;
            --tab-width: 45px;
        }

        body {
            background-color: #E6E6E6;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: 'EB Garamond', serif;
        }

        /* LE CARNET - Structure identique au Suivi de Candidature */
        #soma-container {
            width: 850px;
            height: 600px;
            background: var(--bg-page);
            position: relative;
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
            display: flex;
            border-radius: 4px;
        }

        /* NAVIGATION LATÉRALE (Copie 100% de ta Photo 3) */
        .sidebar-nav {
            position: absolute;
            right: calc(var(--tab-width) * -1); /* Sortie du carnet à droite */
            top: 30px;
            display: flex;
            flex-direction: column;
            gap: 2px;
        }

        .tab {
            width: var(--tab-width);
            height: 65px;
            border-radius: 0 8px 8px 0;
            cursor: pointer;
            transition: all 0.2s ease;
            box-shadow: 2px 2px 5px rgba(0,0,0,0.05);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0.9;
        }

        .tab:hover {
            width: 55px; /* Petit effet de glissement au survol */
            opacity: 1;
        }

        /* PAGE DE COUVERTURE FIXE (Inspiration Photo 2) */
        .cover-page {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            padding: 60px 0;
            border-right: 1px solid rgba(0,0,0,0.05);
        }

        .main-title {
            font-size: 38px;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: var(--ink);
            margin: 0;
        }

        .user-input-line {
            width: 300px;
            border-bottom: 1.5px solid var(--ink);
            text-align: center;
            padding-bottom: 5px;
            color: #A0A0A0;
            font-size: 14px;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .brand-footer {
            font-size: 22px;
            letter-spacing: 8px;
            color: var(--ink);
        }

        /* TEXTE TOURNER LA PAGE (Vertical à droite) */
        .turn-page-hint {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%) rotate(90deg);
            font-size: 10px;
            letter-spacing: 3px;
            color: var(--ink);
            text-transform: uppercase;
            white-space: nowrap;
        }

    </style>
</head>
<body>

<div id="soma-container">
    <div class="sidebar-nav">
        <div class="tab" style="background: #E8B1B1;"></div> <div class="tab" style="background: #F2C38D;"></div> <div class="tab" style="background: #F2EB8D;"></div> <div class="tab" style="background: #BAF28D;"></div> <div class="tab" style="background: #8DBAF2;"></div> <div class="tab" style="background: #C5B1E8;"></div> </div>

    <div class="cover-page">
        <h1 class="main-title">Sōma Book</h1>
        
        <div class="user-input-line">Prénom et Nom</div>

        <div class="turn-page-hint">Tourner la page →</div>

        <div class="brand-footer">SŌMA</div>
    </div>
</div>

</body>
</html>
