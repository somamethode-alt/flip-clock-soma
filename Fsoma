<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
:root {
    /* Palette Sōma - Accordée au widget horloge */
    --soma-lavender: #e7def6;
    --soma-peach: #fbe4d8;
    --soma-mint: #e2f3e8;
    --soma-text: #4b4453;
    --soma-accent: rgba(75, 68, 83, 0.1);
}

body {
    background: #ffffff;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 40px;
    height: 100vh;
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

/* LE DOSSIER SŌMA EXCLUSIF */
.soma-folder {
    width: 160px;
    height: 130px;
    position: relative;
    border-radius: 18px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    cursor: default;
    transition: transform 0.3s ease;
    box-shadow: 0 10px 25px rgba(0,0,0,0.03);
}

/* Onglet supérieur minimaliste */
.soma-folder::before {
    content: "";
    position: absolute;
    top: -10px;
    left: 15px;
    width: 50px;
    height: 20px;
    background: inherit;
    border-radius: 10px 10px 0 0;
    z-index: -1;
}

/* Variantes de textures Sōma */
.v-lavender { background: var(--soma-lavender); }
.v-peach { background: var(--soma-peach); }
.v-mint { background: var(--soma-mint); }

/* Motif discret (Gingham léger) */
.pattern-soft {
    background-image: linear-gradient(var(--soma-accent) 1px, transparent 1px),
                      linear-gradient(90deg, var(--soma-accent) 1px, transparent 1px);
    background-size: 15px 15px;
}

.icon-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
}

.symbol {
    font-size: 28px;
    opacity: 0.8;
    color: var(--soma-text);
}

.folder-name {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--soma-text);
}

.brand-tag {
    position: absolute;
    bottom: 12px;
    font-size: 7px;
    font-weight: 700;
    letter-spacing: 1px;
    opacity: 0.4;
    color: var(--soma-text);
}

/* Décoration "Nœud" minimaliste pour l'esthétique Pinterest */
.ribbon {
    position: absolute;
    top: 10px;
    right: 10px;
    font-size: 14px;
    opacity: 0.6;
}
</style>
</head>
<body>

    <div class="soma-folder v-lavender pattern-soft">
        <div class="ribbon">🎀</div>
        <div class="icon-content">
            <div class="symbol">✦</div>
            <div class="folder-name">Studio</div>
        </div>
        <div class="brand-tag">SŌMA</div>
    </div>

    <div class="soma-folder v-peach">
        <div class="icon-content">
            <div class="symbol">☁</div>
            <div class="folder-name">Dreams</div>
        </div>
        <div class="brand-tag">SŌMA</div>
    </div>

    <div class="soma-folder v-mint pattern-soft">
        <div class="icon-content">
            <div class="symbol">✎</div>
            <div class="folder-name">Archive</div>
        </div>
        <div class="brand-tag">SŌMA</div>
    </div>

</body>
</html>
