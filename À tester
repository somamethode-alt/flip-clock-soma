<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Sōma Book - Dashboard</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600&family=EB+Garamond:ital,wght@0,400;1,400&display=swap');

        :root {
            /* Palette Globale Sōma Book */
            --bg-paper: #FDFBF7;
            --bg-grimoire: #F4E4BC;
            --ink: #3D2B1F;
            --gold: #D4AF37;
            --wood-dark: #5D4037;
            --border-kraft: #DCD1B3;
            
            /* Couleurs des Onglets (Modifiables) */
            --tab-rose: #F28D8D;
            --tab-sable: #F2C38D;
            --tab-jaune: #F2EB8D;
            --tab-sauge: #BAF28D;
            --tab-azur: #8DF2E3;
            --tab-nuit: #8DBAF2;
        }

        body {
            margin: 0;
            background: #1a1a1a; /* Fond externe sombre */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            font-family: 'EB Garamond', serif;
        }

        /* LE CONTENEUR PRINCIPAL (Effet carnet en main) */
        #soma-book {
            width: 90vw;
            height: 85vh;
            background: var(--bg-paper);
            background-image: url('https://www.transparenttextures.com/patterns/paper-fibers.png'); /* Grain de papier visible */
            border: 1px solid var(--border-kraft);
            box-shadow: 0 10px 50px rgba(0,0,0,0.5);
            display: flex;
            position: relative;
            border-radius: 2px 10px 10px 2px;
        }

        /* BARRE LATÉRALE D'ONGLETS (Type Photo 3 / Sōma Navigation) */
        .soma-nav {
            position: absolute;
            right: -45px;
            top: 20px;
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .soma-tab {
            width: 45px;
            height: 70px;
            writing-mode: vertical-rl;
            text-align: center;
            color: var(--ink);
            font-size: 13px;
            font-weight: bold;
            border-radius: 0 5px 5px 0;
            cursor: pointer;
            transition: 0.3s;
            box-shadow: 2px 2px 5px rgba(0,0,0,0.1);
        }

        .soma-tab:hover { transform: translateX(5px); }

        /* ZONE DE CONTENU (PAGE) */
        .book-page {
            flex: 1;
            padding: 60px;
            overflow-y: auto;
            position: relative;
        }

        /* TITRE SŌMA BOOK */
        .book-title {
            font-family: 'Dancing Script', cursive;
            font-size: 60px;
            color: var(--ink);
            text-align: center;
            margin-bottom: 5px;
        }

        .user-name {
            text-align: center;
            font-style: italic;
            color: var(--wood-dark);
            margin-bottom: 50px;
            border-bottom: 1px solid var(--border-kraft);
            display: block;
            width: 200px;
            margin-left: auto;
            margin-right: auto;
        }

        /* L'ÉTAGÈRE (Design Photo 2) */
        .shelf-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 40px;
        }

        .shelf-block {
            width: 300px;
            border-bottom: 6px solid var(--wood-dark);
            display: flex;
            align-items: flex-end;
            padding-bottom: 2px;
            position: relative;
        }

        .book-blank {
            width: 35px;
            height: 120px;
            background: rgba(255,255,255,0.4);
            border: 1px solid var(--border-kraft);
            margin-right: 5px;
            transition: 0.4s;
        }

        .book-blank:hover {
            background: white;
            transform: translateY(-5px);
        }

        .window-arch {
            width: 120px;
            height: 180px;
            border: 4px solid var(--wood-dark);
            border-radius: 60px 60px 0 0;
            background: #0a0a1a;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-size: 30px;
        }

        /* NAVIGATION "TOURNER LA PAGE" */
        .page-footer {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            font-family: 'Dancing Script', cursive;
            font-size: 22px;
            color: var(--wood-dark);
            cursor: pointer;
            border-bottom: 1px solid var(--wood-dark);
        }

    </style>
</head>
<body>

<div id="soma-book">
    <div class="soma-nav">
        <div class="soma-tab" style="background: var(--tab-rose);">Couverture</div>
        <div class="soma-tab" style="background: var(--tab-sable);">Dashboard</div>
        <div class="soma-tab" style="background: var(--tab-jaune);">Studio</div>
        <div class="soma-tab" style="background: var(--tab-sauge);">Réseaux</div>
        <div class="soma-tab" style="background: var(--tab-azur);">Business</div>
        <div class="soma-tab" style="background: var(--tab-nuit);">Calendrier</div>
    </div>

    <div class="book-page">
        <div class="book-title">Sōma Book</div>
        <span class="user-name">Propriété de [Votre Nom]</span>

        <div class="shelf-container">
            <div class="shelf-block">
                <div class="book-blank"></div>
                <div class="book-blank"></div>
            </div>
            
            <div class="window-arch">🌙</div>

            <div class="shelf-block">
                <div class="book-blank"></div>
                <div class="book-blank"></div>
            </div>
        </div>

        <div class="shelf-container" style="margin-top: 80px;">
            <div class="shelf-block" style="width: 80%;">
                <div class="book-blank"></div>
                <div class="book-blank"></div>
                <div class="book-blank"></div>
                <div class="book-blank"></div>
                <div class="book-blank"></div>
            </div>
        </div>

        <div class="page-footer">Tourner la page ➔</div>
    </div>
</div>

</body>
</html>
